# Generated Xcode project (regenerate with `xcodegen`)
*.xcodeproj
*.xcworkspace

# Xcode build artifacts
build/
DerivedData/
*.xcuserdata

# Ghostty build output (rebuild with `zig build`)
ghostty/zig-out/
ghostty/.zig-cache/

# Node dependencies (installed via bun in CI)
node_modules/

# macOS
.DS_Store
*.swp
*~

# Ghostty xcframework is built from submodule, not committed
# ghostty/macos/GhosttyKit.xcframework is generated by zig build
default.profraw
